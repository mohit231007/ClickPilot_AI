# Contributing

1. Create a feature branch.
2. Keep original benchmark facts separate from synthetic-demo metrics.
3. Add or update tests for model or API behavior.
4. Run `ruff check .`, `pytest -q`, and `python -m compileall src api app scripts`.
5. Document any change that alters the model contract or responsible-use boundary.
